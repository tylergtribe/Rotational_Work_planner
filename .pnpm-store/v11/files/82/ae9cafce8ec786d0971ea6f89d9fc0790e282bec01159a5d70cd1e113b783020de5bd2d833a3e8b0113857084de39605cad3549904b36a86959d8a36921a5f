# @openint/connectors-sdk (published as `@replit/connectors-sdk`)

Public consumer SDK used by Repls to resolve identity, list connections, and proxy requests through OpenInt. Not the framework for authoring connectors — that's `@openint/cdk`; this is the client-side SDK end-user code uses to call OpenInt.
